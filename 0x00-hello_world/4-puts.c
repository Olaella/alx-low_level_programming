#include <stdio.h>
/**
 * main -A program that prints a line with puts function
 * return: 0; on successful execution, non-zero on errors.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
return : (0);
}
